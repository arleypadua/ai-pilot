{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "AutoPilotConfig",
  "description": "Configuration schema for Agent Auto-Pilot",
  "type": "object",
  "properties": {
    "repository": {
      "type": "string",
      "pattern": "^[a-zA-Z0-9_.-]+/[a-zA-Z0-9_.-]+$",
      "description": "Target GitHub repository in 'owner/repo' format (e.g. 'arleypadua/imagos')"
    },
    "targetSpec": {
      "oneOf": [
        { "type": "integer" },
        { "type": "array", "items": { "type": "integer" } }
      ],
      "description": "Optional issue number(s) of Spec(s) to scope execution strictly to child tickets"
    },
    "targetSpecs": {
      "type": "array",
      "items": {
        "type": "integer"
      },
      "description": "Optional list of Spec issue numbers to scope execution strictly to child tickets"
    },
    "baseBranch": {
      "type": "string",
      "default": "main",
      "description": "The base branch to branch off of and merge PRs into"
    },
    "maxConcurrency": {
      "type": "integer",
      "minimum": 1,
      "default": 2,
      "description": "Maximum number of parallel agent worktrees to run simultaneously"
    },
    "pollIntervalSeconds": {
      "type": "integer",
      "minimum": 5,
      "default": 30,
      "description": "Frequency in seconds to poll GitHub issues"
    },
    "extraPrompt": {
      "type": "string",
      "description": "Additional custom instructions or context injected into every agent prompt across the repository (e.g. environment setup, copy untracked .env files, styling rules)"
    },
    "runner": {
      "type": "string",
      "enum": ["claude", "agy", "pi", "custom"],
      "default": "claude",
      "description": "Coding agent runner to execute"
    },
    "customRunnerCommand": {
      "type": "string",
      "description": "Shell command template if runner is set to 'custom'"
    },
    "runnerConfig": {
      "type": "object",
      "description": "Optional runner-specific configurations (e.g. model, effort)",
      "properties": {
        "agy": {
          "type": "object",
          "description": "Antigravity CLI runner configuration",
          "properties": {
            "model": {
              "type": "string",
              "description": "Model selection for Antigravity runner"
            },
            "effort": {
              "type": "string",
              "description": "Effort / reasoning intensity level"
            }
          },
          "additionalProperties": true
        }
      },
      "additionalProperties": true
    },
    "autoMerge": {
      "type": "boolean",
      "default": true,
      "description": "Whether to automatically merge the Pull Request if tests pass"
    },
    "mergeMethod": {
      "type": "string",
      "enum": ["squash", "merge", "rebase"],
      "default": "squash",
      "description": "Git merge strategy for Pull Requests"
    },
    "cleanupWorktreeOnClose": {
      "type": "boolean",
      "default": true,
      "description": "Whether to automatically delete the worktree and branch once the issue is merged and closed"
    },
    "remote": {
      "type": "object",
      "description": "Remote control and notification settings (e.g. Telegram bot)",
      "properties": {
        "enabled": {
          "type": "boolean",
          "default": false,
          "description": "Whether remote control is enabled"
        },
        "provider": {
          "type": "string",
          "enum": ["telegram", "slack", "discord"],
          "default": "telegram",
          "description": "Remote control provider"
        },
        "telegram": {
          "type": "object",
          "description": "Telegram provider configuration",
          "properties": {
            "botTokenEnv": {
              "type": "string",
              "default": "TELEGRAM_BOT_TOKEN",
              "description": "Environment variable name containing the Telegram bot token"
            },
            "allowedUserIds": {
              "type": "array",
              "items": { "type": "integer" },
              "description": "Whitelisted Telegram user IDs allowed to interact with the bot"
            },
            "defaultChatId": {
              "oneOf": [
                { "type": "integer" },
                { "type": "string" }
              ],
              "description": "Default Telegram chat ID or channel username for milestone notifications"
            },
            "notifications": {
              "type": "object",
              "description": "Notification event toggles",
              "properties": {
                "needsInfo": {
                  "type": "boolean",
                  "default": true,
                  "description": "Notify when an agent requires developer feedback (needs-info)"
                },
                "quotaPaused": {
                  "type": "boolean",
                  "default": true,
                  "description": "Notify when quota limit is reached and workers are paused"
                },
                "taskCompleted": {
                  "type": "boolean",
                  "default": true,
                  "description": "Notify when a task is completed and PR is created/merged"
                },
                "specCompleted": {
                  "type": "boolean",
                  "default": true,
                  "description": "Notify when all child tickets of a parent spec are completed"
                }
              },
              "additionalProperties": false
            }
          },
          "additionalProperties": false
        }
      },
      "additionalProperties": false
    },
    "quota": {
      "type": "object",
      "description": "Quota and rate limit settings",
      "properties": {
        "pauseOnLimit": {
          "type": "boolean",
          "default": true,
          "description": "Automatically pause active workers when quota threshold is reached or exceeded"
        },
        "utilizationThresholdLimit": {
          "type": "number",
          "minimum": 0.01,
          "maximum": 1.0,
          "default": 0.85,
          "description": "Proactive quota utilization threshold to pause task execution (e.g. 0.70 for 70%)"
        },
        "utilizationThreshold": {
          "type": "number",
          "minimum": 0.01,
          "maximum": 1.0,
          "default": 0.85,
          "description": "Alias for utilizationThresholdLimit"
        },
        "proxyPort": {
          "type": "integer",
          "default": 9876,
          "description": "Port for optional rate-limit interceptor proxy"
        }
      },
      "additionalProperties": false
    },
    "labels": {
      "type": "object",
      "description": "Canonical triage label mappings",
      "properties": {
        "readyForAgent": {
          "type": "string",
          "default": "ready-for-agent",
          "description": "Label indicating an issue is ready for autonomous agent execution"
        },
        "needsInfo": {
          "type": "string",
          "default": "needs-info",
          "description": "Label indicating agent has paused and requested human feedback"
        },
        "readyForHuman": {
          "type": "string",
          "default": "ready-for-human",
          "description": "Label indicating issue requires human review or manual intervention"
        },
        "needsTriage": {
          "type": "string",
          "default": "needs-triage",
          "description": "Label for newly filed issues awaiting triage"
        },
        "wontfix": {
          "type": "string",
          "default": "wontfix",
          "description": "Label for non-actionable issues"
        }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": false
}
