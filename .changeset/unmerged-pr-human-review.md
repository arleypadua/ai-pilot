---
"imagos": patch
---

fix: automatically detect unmerged PRs upon agent completion, attempt auto-merge if enabled, transition tasks to `ready-for-human` on GitHub to prevent infinite re-dispatch, and accurately display in-review worktrees in the dashboard.
