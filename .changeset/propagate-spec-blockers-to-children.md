---
"imagos": patch
---

fix: propagate parent spec blockers down to all child tickets in the DAG, support native GitHub issue relationships (`blockedBy`, `parent`, `subIssues`) via GraphQL, and automatically prune completed or closed specs from the active target scope with activity logging.
